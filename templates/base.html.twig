<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>{% block title %}Tasklinker : ~Nompage {% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        {# --- CSS externes --- #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css"
              integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w=="
              crossorigin="anonymous" referrerpolicy="no-referrer" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
              integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
        <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
        <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
        {# --- CSS du projet --- #}
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('assets/styles/style.css') }}">
        {% endblock %}

        {# --- Scripts personnalisés (ImportMap) --- #}
{#        {% block importmap %}#}
{#            {{ importmap('app') }}#}
{#        {% endblock %}#}

        {% block javascripts %}
            {# Optionnel : scripts spécifiques supplémentaires ici #}
        {% endblock %}
    </head>
    <body>
        <div id="main-container">
            {% block navigation %}
            <nav>
                <a href="{{ path("projet.index") }}" id="logo">
                    <img src="{{ asset('assets/images/logo.png') }}" alt="Logo TaskLinker" />"
                </a>
                <ul id="main-menu">
                    <li class="{{ app.current_route starts with 'projet' ? 'active': '' }}">
                        <a href="{{ path("projet.index") }} ">
                            <i class="fa-solid fa-list-check"></i> Projets
                        </a>
                    </li>
                    <li class="{{ app.current_route starts with 'employe' ? 'active': '' }}">
                        <a href="{{ path("employe") }}">
                            <i class="fa-regular fa-user"> </i> Équipe
                        </a>
                    </li>
                </ul>
            </nav>
            {% endblock %}
            <main>
                <header>
                    <div id="current-user" class="d-flex align-items-center justify-content-end">
                        {% if app.user %}
                            <span class="me-3 user-name">
                                <i class="fa-regular fa-user"></i>
                                {{ app.user.prenom }} {{ app.user.nom }}
                            </span>
                            <a href="{{ path('logout') }}" class="btn btn-outline-danger btn-sm logout-link">
                                <i class="fa-solid fa-right-from-bracket"></i> Déconnexion
                            </a>
                        {% endif %}
                    </div>

                    <div id="title-page">
                        <h1>{% block Nompage %} {% endblock %}</h1>
                        {% block listemploye %}

                        {% endblock %}
                    </div>
                </header>
                {# Bloc de contenu principal #}
                 <div id="content">
                     {%  include 'commun/flash.html.twig' %}
                     {% block body %}

                     {% endblock %}
                 </div>
            </main>
        </div>
        {# Bloc modal global réutilisable #}
        {% block modal %}
            <div class="modal fade" id="genericConfirmModal" tabindex="-1" aria-labelledby="genericConfirmLabel" aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header bg-light">
                            <h5 class="modal-title" id="genericConfirmLabel">Titre par défaut</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
                        </div>
                        <div class="modal-body">
                            <p>Contenu du message par défaut.</p>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                            <a href="#" id="confirmActionBtn" class="btn btn-danger">Confirmer</a>
                        </div>
                    </div>
                </div>
            </div>
        {% endblock %}

        {# jQuery + Select2 JS #}

        <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>

        {# Ton JS perso, chargé après jQuery + Select2 #}
        <script src="{{ asset('assets/js/select2-projet.js') }}"></script>
    </body>
</html>
